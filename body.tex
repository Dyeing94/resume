\logosection{\faGraduationCap}{教育经历}
\datedline{\textbf{福州大学}}{\dateRange{2024.09}{2027.06}}
\datedline{\biInfo{平均绩点}{\textbf{3.36/4.00}}}{}
\datedline{\textbf{湖南理工大学}}{\dateRange{2020.09}{2024.06}}
\datedline{\biInfo{平均绩点}{\textbf{3.23/4.00}}}{}

\logosection{\faTrophy}{奖项荣誉}
\datedline{\textbf{国家励志奖学金}}{2022.11}
\datedline{{“高教杯”全国大学生先进成图技术与产品信息建模创新大赛团队二等奖}}{2022.08}
\datedline{第三届湖南省大学生社团就业创业能力挑战赛“百强社团”}{2021.06}
\datedline{湖南理工大学“三好学生标兵”}{2023.01}
\datedline{福州大学中期学业三等奖学金}{2026.05}

\logosection{\faUniversity}{项目经历}

\datedline{\textbf{\href{https://www.date-match.online/}{DateMatch}} · AI 驱动社交匹配平台，非营利项目，用户规模近 20 万}{\dateRange{2026.01}{至今}}
\datedline{\biInfo{技术栈}{\textbf{PostgreSQL/pgvector, Kafka, Redis, Embedding, Reranker, LLM}}}{}
\begin{itemize}[itemsep=0.5ex, parsep=0ex]
    \item \textbf{分层决策工作流:} 负责「规则筛选 $\rightarrow$ Embedding 召回 $\rightarrow$ Rerank 相容性粗筛 $\rightarrow$ LLM 互补性决策」四阶段链路，将 20 万用户逐层收敛至 Top 200/Top 50；相较基线，单次匹配计算成本降低约 70\%。
    \item \textbf{向量召回优化:} 针对高选择性画像过滤场景选用 IVFFlat（lists=400, probes=30），结合标签预过滤与批量回表；20 万条 1024 维向量下 Recall@200 > 95\%，召回链路 P99 < 80ms。
    \item \textbf{Kafka + Redis 匹配流水线:} 使用 Kafka 将注册、画像更新、Embedding 计算、候选计算拆分为异步事件流，通过 Consumer Group 并行消费与削峰填谷支撑批量匹配任务；同时用 Redis 缓存在线状态、候选集合与近期匹配结果，并结合 TTL 降低重复计算，峰值可平滑处理 3k+/min 匹配请求，重复计算开销下降约 60\%。
\end{itemize}

\datedline{\textbf{LiveGalGame + NarrowArc} ·2.5k stars 实时 AI 对话助手与聊天记录 RAG}{\dateRange{2025.12}{至今} }
\datedline{\biInfo{技术栈}{\textbf{TypeScript, Python, LangGraph, Qdrant, Reranker, RAG}}}{}
\begin{itemize}[itemsep=0.5ex, parsep=0ex]
    \item \textbf{实时决策控制:} 将流式 ASR 增量文本收敛为 committed turn，并以「轮到用户回应 / 连续多句 / 长时沉默」三类门控决定是否触发建议；通过双路音源分离维护双方上下文，避免 partial 文本误触发和角色混淆。
    \item \textbf{低延迟检索增强：} 采用文档 Embedding 异步预计算，在线链路按「Query Embedding $\rightarrow$ 本地 Qdrant Top 50 $\rightarrow$ Reranker Top 5 $\rightarrow$ LLM」执行；端到端 P90 控制在 1.5s 内，整个流程 655-1265ms。
    \item \textbf{Agentic RAG 评测:} 为 NarrowArc 设计 Burst/TopicNode 双层索引及 planner-retriever-grader-explorer-generator 循环；在 RealTalk 50 条叙事问题上，闭环将 Phase Coverage 由 55\% 提升至 85\%，Fuzzy Recall达 78\%，支持叙事结论回溯至原始聊天证据。
\end{itemize}

\logosection{\faFlask}{科研经历}
\datedline{\textbf{基于 LAAF-BI-PINN 的输水隧道水锤作用下安全风险评估研究}}{\dateRange{2026.01}{2027.06}}
\datedline{\biInfo{方法}{\textbf{Python, PINN, 贝叶斯推断, K-Means, 贝叶斯网络}}}{}
\begin{itemize}[itemsep=0.5ex, parsep=0ex]
    \item \textbf{瞬态建模与数据构建:} 面向输水隧道水锤作用下的安全风险评估，建立多工况瞬态分析流程，完成压力、流速与结构响应数据的清洗、归一化及训练样本构建。
    \item \textbf{LAAF-BI-PINN 方法:} 在物理信息神经网络中引入局部自适应激活函数与贝叶斯推断，将控制方程、边界条件和观测数据统一纳入损失函数，实现关键响应预测与不确定性量化。
    \item \textbf{风险评估闭环:} 采用 K-Means 划分典型运行状态，结合贝叶斯网络开展风险推理与敏感性分析，识别关键致险因素并形成风险等级判定流程，为输水隧道安全运行提供决策依据。
\end{itemize}

\logosection{\faCogs}{技术栈}
\begin{itemize}[parsep=0ex, itemsep=0ex, leftmargin=*]
    \item \textbf{编程语言:} Python，TypeScript \quad \textbf{数据库\&中间件:} PostgreSQL、pgvector、Qdrant、Redis、Kafka \quad \textbf{AI/ML:} LLM、LangGraph、PINN、RAG、Embedding、Reranker、IVFFlat、HNSW 
\end{itemize}
